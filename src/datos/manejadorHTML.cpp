#include "manejadorHTML.hpp"
#include "../estructuras/nodo.hpp"
#include <iostream>
#include <list>
#include <vector>

using std::cout;
using std::endl;
using std::list;
using std::string;
using std::vector;

string header = "<!DOCTYPE html>\n"
                "<html>\n<head>\n    <meta charset=\"UTF-8\">\n"
                "    <meta name= \"favoritos.html\" content= \"Favoritos\">\n"
                "    <title>Favoritos</title>\n</head>\n<body>\n"
                "    <h1>Favoritos</h1>\n";

string manejadorHTML::formatearCarpeta(archivo carpeta) {
  string carpetaFormateada;
  return carpetaFormateada;
}
string manejadorHTML::formatearArchivo(archivo a) {
  string archivoFormateada;
  return archivoFormateada;
}
string manejadorHTML::armarCuerpoHTML() {
  string cuerpo;

  return cuerpo;
}
void manejadorHTML::armarHTML() {}

string comentarioPosicion(string contenedor) {
  string comentario;
  return comentario;
}
